##상속(Inherit) %재사용 함수>>클래스>>상속(일반화)
-일반화
-부모의 재산(MV,MM) 사용 허용
-python 형식 : class 클래스명(부모 클래스 명)
-override:상속받은 곳 에서 메소드를 그대로 재정의
 └>MM:현재 객체가 우선>>
 2순위 사용할 땐 super()<부모객체
 self=현재 객체.
 
%ovreload:같은 이름의 매소가 여러개
구분:매개변수의 개수
